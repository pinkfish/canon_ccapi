name: canon_ccapi_example
description: Demonstrates how to use the canon_ccapi plugin.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  canon_ccapi:
    path: ../

  cupertino_icons: ^1.0.3

  upnp:
    git: https://github.com/pinkfish/upnp.dart

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true
