import 'package:flutter/material.dart';

///
/// A nice view of the camera details
///
class CameraView extends StatelessWidget {
  final Uri url;

  const CameraView({required this.url, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
